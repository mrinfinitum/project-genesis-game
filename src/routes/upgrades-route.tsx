import { UpgradesWorkspace, type WorkspaceRouteProps } from "@/routes/workspace-components";

export default function UpgradesRoute(props: WorkspaceRouteProps) {
  return <UpgradesWorkspace {...props} />;
}
