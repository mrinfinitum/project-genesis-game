import { BuildingsWorkspace, type WorkspaceRouteProps } from "@/routes/workspace-components";

export default function EarthRoute(props: WorkspaceRouteProps) {
  return <BuildingsWorkspace {...props} />;
}
