import { EventsWorkspace, type WorkspaceRouteProps } from "@/routes/workspace-components";

export default function EventsRoute(props: WorkspaceRouteProps) {
  return <EventsWorkspace {...props} />;
}
