import { ResearchWorkspace, type WorkspaceRouteProps } from "@/routes/workspace-components";

export default function ResearchRoute(props: WorkspaceRouteProps) {
  return <ResearchWorkspace {...props} />;
}
