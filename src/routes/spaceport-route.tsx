import { SpaceportWorkspace, type WorkspaceRouteProps } from "@/routes/workspace-components";

export default function SpaceportRoute(props: WorkspaceRouteProps) {
  return <SpaceportWorkspace {...props} />;
}
